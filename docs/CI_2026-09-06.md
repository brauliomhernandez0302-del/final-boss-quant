# Evidencia de CI — commit `21784fb`

Reproducción local del workflow `.github/workflows/ci.yml`, job `python`, sobre
un checkout limpio del commit evaluado. No es la corrida de GitHub Actions: esa
requiere un `push`, y la rama está 36 commits por delante de `origin`.

## Condiciones, iguales a las del CI

| | |
|---|---|
| commit | `21784fbe898b76ee5a87fb05337a207b207fb93f` |
| checkout | `git worktree add --detach` — árbol limpio, sólo lo que está en git |
| bases de datos | **0 archivos `.db`** — el CI no las tiene, y no debe necesitarlas |
| entorno | venv nuevo, `pip install -r requirements.txt -r requirements-dev.txt` |
| Python | 3.12.3 (el workflow pide `3.12`) |
| comando | `python -m pytest -q` |

## Resultado

```
1040 passed, 25 skipped, 11 warnings in 154.53s (0:02:34)
exit code 0
```

## Comparación con la última verificación registrada

El propio workflow anota: *"Verificado en un clon limpio el 2026-08-02: 857
passed, 25 skipped, 0 fallos."*

| | 2026-08-02 | 2026-09-06 |
|---|---|---|
| passed | 857 | **1040** |
| skipped | 25 | **25** |
| fallos | 0 | **0** |

Los 25 saltados son los mismos: los tests que necesitan un estado que sólo
existe después de un backtest completo. Que el número no se haya movido es la
señal de que los 183 tests nuevos corren de verdad en CI y no se están saltando
por falta de datos.

## Nota sobre el árbol de trabajo

Corriendo la suite **con** las bases de datos presentes (el worktree de
recuperación, no el CI) el resultado es **1047 passed, 18 skipped**: siete de
los saltados dejan de saltarse porque encuentran los datos que necesitan.
Ninguno falla en ninguno de los dos modos.


---

## Segunda corrida — commit `e2e7602` (conciliación + migración de features)

Mismas condiciones: checkout limpio con `git worktree add --detach`, **0
archivos `.db`**, venv nuevo desde `requirements.txt`, Python 3.12.3,
`python -m pytest -q`.

```
1049 passed, 25 skipped, 11 warnings in 148.29s
exit code 0
```

| | 2026-08-02 | 21784fb | e2e7602 |
|---|---|---|---|
| passed | 857 | 1040 | **1049** |
| skipped | 25 | 25 | **25** |
| fallos | 0 | 0 | **0** |

Los saltados siguen siendo 25: los tests nuevos de conciliación y de veredictos
versionados corren en CI sin datos.

Con las bases presentes (worktree de recuperación): **1056 passed, 18 skipped**.


---

## Tercera corrida — commit `537f3de` (candidato v1 de P(gana el local))

Mismas condiciones: checkout limpio, **0 archivos `.db`**, venv nuevo, Python
3.12.3, `python -m pytest -q`.

```
1063 passed, 25 skipped, 11 warnings in 146.41s
exit code 0
```

| | 2026-08-02 | 21784fb | e2e7602 | 537f3de |
|---|---|---|---|---|
| passed | 857 | 1040 | 1049 | **1063** |
| skipped | 25 | 25 | 25 | **25** |
| fallos | 0 | 0 | 0 | **0** |

Los 14 tests del modelo —compuerta temporal, los dos controles de fuga, ridge
sin penalizar el intercepto, la copia vigilada de `regress`— corren en CI sin
bases de datos: el contador de saltados no se movió.

Con las bases presentes (worktree de recuperación): **1070 passed, 18 skipped**.
